let arr = [1,2,3];
      let rev = [];
      let i;
      for(i = arr.length-1; i>=0; i--){
         rev.push(arr[i]);
      }
      for(let i = 0; i<rev.length; i++){
         console.log(rev[i] + " ");
      }
